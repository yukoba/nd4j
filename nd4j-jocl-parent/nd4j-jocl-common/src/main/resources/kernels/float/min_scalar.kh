#include "scalar.h"

__global  float op(float d1,float d2,global float *params) {
    if(d1 < d2)
      return 1;
   return 0;
}


__kernel void min_scalar_float(int n, int idx,float dx,global float *dy,int incy,global float *params,global float *result) {
       transform(n,idx,dx,dy,incy,params,result);
 }


