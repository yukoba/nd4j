#include "transform.h"

__global  float op(float d1,global float *params) {
        return sqrt(d1);
}


__kernel void sqrt_strided_float(int n,int idx,global float *dy,int incy,global float *params,global float *result) {
       transform(n,idx,dy,incy,params,result);

 }